# Memory Index

- [Conductor prod logs status mis-mapped](conductor-prod-logs-status-mismapped.md) — DD shows Conductor INFO logs as critical/emergency; trust custom.level not DD status; judge availability via replica/crashloop metrics
- [IDI call outcome double-counting](idi-call-outcome-double-counting.md) — "Call outcome recorded" fires multiple times/call; event-count dashboards over-state outcomes; use last-per-session + treat missing as abandoned
- [IDI DTMF feasibility](idi-dtmf-feasibility.md) — keypad capture viable: Twilio→LiveKit leg proven to deliver sip_dtmf_received (manual test); Ooma hop still untested; synthetic harness can't test DTMF
- [IDI Jun26 conversion root causes](idi-jun26-conversion-root-causes.md) — Jun26 audio analysis (34 calls, 0 bookings): name-SPELLING is #1 fixable loss (not digit capture); no-progress floor over-escalates progressing callers + mislabels phase=greeting; new-vs-returning is LLM-gated; natural-speech-first name capture already tried+regressed; LiveKit AssemblyAI can't swap prompt/keyterms mid-session
- [IDI funnel measurement gotchas](idi-funnel-measurement-gotchas.md) — DD funnel: call_finished wide event (idi-worker) is the facetable spine; @final_phase now facetable on call_finished (PR #4150, worker ≥6e15c2e7b) but NOT on the voice-agent completion log (repr-string); completion event double-emits → DISTINCT lk_session_id; idi-worker vs idi-voice-agent deploy independently (confirm both versions)
- [IDI turn-detector trap](idi-turn-detector-trap.md) — turn_detection=EnglishModel() dead-airs every IDI call (turn-detector extra NOT installed; Dockerfile prefetch comment stale); OSMI #4088 precedent; add extra+rebuild+verify before prod

- [IDI CAS shared SystemUserId](idi-cas-shared-system-user.md) — booking owner-CAS uses shared SYSTEM_USER_ID=567; can't tell two concurrent voice callers apart; book verify pairs it w/ PatientId
- [IDI create/intake confirm consolidation](idi-create-intake-confirm-consolidation.md) — deferred: one code-emitted demographics readback to kill the ~11s LLM-turn latency; NOT AUTO-891; constraints to preserve
- [IDI sono exam-definition unverified](idi-sono-exam-definition-unverified.md) — SHIPPED TO PROD (AUTO-869, PR #3938→760fcaea9, prod deploy PRs #3944/#3945, on-prem redeployed): US-0 books ONLY sonohysterography; obstetric routes to regular US w/ full-bladder prep; new no-bladder sono prep; dedicated `sono` skill column distinct from `obs`; incl. local-rig + idi deploy mechanics

- [IDI call_id is PHI (fixed)](idi-call-id-is-phi.md) — runtime call_id is now room.sid (PHI-free); caller phone is room_name / calls.session_id (PHI); room.sid migration shipped
- [IDI LK-down failover coverage](idi-lk-down-failover-coverage.md) — AUTO-811: Twilio failover can't catch "no agent" (LK 200-OKs regardless); EKS-down needs a health-gated inbound (D) + agent watchdog (C)
- [IDI patient_id is NOT PHI](idi-patient-id-not-phi.md) — Radiant-internal unique ID; safe to log unredacted; the redact_patient_id bullet is invalid
- [IDI local rig dispatch contention](idi-local-rig-dispatch-contention.md) — scale dev idi-voice-agent to 0 (--context dev-cac1!) before a local rig test or calls route to the cloud worker; ArgoCD resyncs it back
- [IDI SIP dispatch empty_timeout](idi-sip-dispatch-empty-timeout.md) — dev+prod dispatch rules now have empty/departure timeouts (LiveKit-side) to auto-GC abandoned rooms; fixes the zombie-room leak (code half in PR #3827)
- [IDI on-prem DD logs need both envs](idi-onprem-dd-logs-both-envs.md) — VM runs dev+prod Radiant workers; setup-datadog.ps1 writes a single-env log source (overwrites); need a 4-source conf + `Restart-Service datadogagent` to load it
- [IDI non-bookable/FAQ needs prompt grounding](idi-nonbookable-faq-needs-prompt-grounding.md) — agent answers these conversationally at the greeting; render-only changes don't reach callers without catalog-fact + greeting grounding
- [AUTO-706 wide-events plan](auto-706-wide-events-plan.md) — IDI canonical-log-lines observability: direction, correlation_id design, execution order, open questions
- [IDI cloud provisioning gaps](idi-cloud-provisioning-gaps.md) — worker role grows reactively; RDS then Bedrock grants missing, surface only on real cloud calls — add to both envs
- [IDI prod dbt ownership gotcha](idi-prod-dbt-ownership-gotcha.md) — manual prod dbt build → human-owned UC objects → hourly SP can't CREATE OR REPLACE → prod pipeline silently red
- [AUTO-866 x-ray sibling out of scope](auto-866-xray-sibling-out-of-scope.md) — mid-registration x-ray walk-in interrupt is out of scope by IDI AgentTask architecture; track separately, not a one-line fix
- [IDI vascular routing gap](idi-vascular-routing-gap.md) — OPEN/unfixed: detection is substring-only (doppler/vascular); jugular/AAA/ankle-brachial/venous/arterial/"carotid US" leak into a Spadina booking; vascular is Coxwell-only; same shape as the sono bug
- [IDI on-prem mirror closure](idi-onprem-mirror-closure.md) — which phone_booking modules are vendored on-prem (sono/locations/activities/models/radiant_client → VM redeploy) vs cloud-only (exam_config/prep_resolver/voice_* → ships on image); decides redeploy need
- [IDI sono resource identity](idi-sono-resource-identity.md) — sono = dedicated resource US 0 (id 346, TimeScale 10) at Spadina; detect by resource identity not the SONO header (GUI-only, absent from WCF); cloud-side logic, but locations→sono import means the vendored on-prem mirror needs sono.py (and had silently drifted)
- [gh CLI host override](gh-cli-host-override.md) — gh fails on GH_HOST=github.com-ph (invalid token) in these worktrees; run `env GH_HOST=github.com gh <cmd> --repo ph-conductor/conductor ...`; av not initialized here (no av.db)
